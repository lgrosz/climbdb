services:
  postgres:
    build:
      target: with-pgtap
      additional_contexts:
        postgis: https://git.osgeo.org/gitea/postgis/postgis.git#3.6.0
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_DB
      - POSTGRES_USER
      - POSTGRES_PASSWORD
    healthcheck:
      test: pg_isready
      interval: 10s
      timeout: 5s
      retries: 5

