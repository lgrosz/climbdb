BEGIN;
SELECT plan(10);

SELECT has_table( 'climb_grades' );

SELECT has_column( 'climb_grades', 'id' );
SELECT col_is_pk( 'climb_grades', 'id' );
SELECT col_type_is('climb_grades', 'id', 'integer');

SELECT has_column( 'climb_grades', 'climb_id' );
SELECT col_not_null( 'climb_grades', 'climb_id', 'integer' );
SELECT fk_ok( 'climb_grades', 'climb_id', 'climbs', 'id' );

SELECT has_column( 'climb_grades', 'grade' );
SELECT col_type_is('climb_grades', 'grade', 'grade');

SELECT col_is_unique( 'climb_grades',  ARRAY['climb_id', 'grade'] );

SELECT * FROM finish();
ROLLBACK;
