BEGIN;
SELECT plan(2);

SELECT has_schema( 'climb' );

SELECT tables_are(
    'climb',
    ARRAY[
        'regions',
        'climbs',
        'crags',
        'formations',
        'sectors'
    ]
);

SELECT * from finish();
ROLLBACK;

