BEGIN;
SELECT plan(2);

SELECT has_schema( 'climb' );

SELECT tables_are(
    'climb',
    ARRAY[
        'regions',
        'climbers',
        'climbs',
        'crags',
        'formations',
        'formations_in_image',
        'sectors'
    ]
);

SELECT * from finish();
ROLLBACK;

