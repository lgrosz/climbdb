BEGIN;
SELECT plan(10);

SELECT has_table( 'ascent_grades' );

SELECT has_column( 'ascent_grades', 'id' );
SELECT col_is_pk( 'ascent_grades', 'id' );
SELECT col_type_is('ascent_grades', 'id', 'integer');

SELECT has_column( 'ascent_grades', 'ascent_id' );
SELECT col_not_null( 'ascent_grades', 'ascent_id', 'integer' );
SELECT fk_ok( 'ascent_grades', 'ascent_id', 'ascents', 'id' );

SELECT has_column( 'ascent_grades', 'grade' );
SELECT col_type_is('ascent_grades', 'grade', 'grade');

SELECT col_is_unique( 'ascent_grades',  ARRAY['ascent_id', 'grade'] );

SELECT * FROM finish();
ROLLBACK;
