BEGIN;
SELECT * from plan(1);

SELECT tables_are(
    'climb',
    ARRAY[
        'regions',
        'climbs',
        'crags',
        'formations',
        'sectors'
    ]
);

SELECT * from finish();
ROLLBACK;

