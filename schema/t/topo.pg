BEGIN;
SELECT plan(2);

SELECT has_schema( 'topo' );

SELECT tables_are(
    'topo',
    ARRAY[
        'topos',
        'image_features',
        'path_features'
    ]
);

SELECT * from finish();
ROLLBACK;


